[project]
name = "rest-api-runner"
version = "0.1.0"
description = "Place button to run REST API calls"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "requests>=2.33.1",
    "streamlit>=1.57.0",
    "uvicorn>=0.46.0",
]
